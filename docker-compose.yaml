version: '3'

services:
  # database:
  #   image: mysql
  #   container_name: filament-database
  #   environment:
  #     MYSQL_DATABASE: "inventory"
  #     MYSQL_USER: admin
  #     MYSQL_PASSWORD: password
  #     MYSQL_ROOT_PASSWORD: password

  #   ports:
  #     - 3306:3306

  #   volumes:
  #     - ./mysql:/var/lib/mysql

  database:
    image: postgres:15.3
    container_name: filament-database

    environment:
      POSTGRES_DB: inventory
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password

    ports:
      - 5432:5432
    
    volumes:
      - ./postgresql:/var/lib/postgresql/data
